Goal: Deliver a read-only source-model canvas that renders the current live workspace, its source memberships, and governed relationship trust without permitting edits.

REPAIR REQUIRED

## Repair Blocker

`frontend/frontend/src/features/data-model/SourceModelCanvas.jsx` still allows a suggested relationship to appear executable. The `isExecutable` predicate checks activation, confirmation, validation state, and cardinality, but omits `!rel.is_suggested`. A saved relationship may retain `is_suggested: true` after it is confirmed and activated, so that reachable state currently receives the green animated executable treatment and suppresses the `Suggested` status that the component already collects. This violates the literal trust rule that suggested records must remain visibly non-executable.

Target only `frontend/frontend/src/features/data-model/SourceModelCanvas.jsx` for this repair. Read `AGENTS.md`, `project_docs/INDEX.md`, `project_docs/active/README.md`, `project_docs/active/active_gate/README.md`, `project_docs/active/contracts/multiple_data_source_relationships.md`, and `project_docs/active/rules/CODEX_FRONTEND_GUARDRAIL_READ_FIRST.md` before editing.

Treat backend readiness as `backend_contract_ready`. Preserve `source`, `workspace`, and `analysis_context` from a successful `POST /api/upload` response in frontend state without removing any existing upload fields. Use `analysis_context.workspace_id` as the only current workspace identity. Load `GET /api/data-workspaces/<workspace_id>` for `workspace.sources[]`, then load `GET /api/data-workspaces/<workspace_id>/relationships` for `relationships[]`. If source detail is needed, use `GET /api/data-workspaces/<workspace_id>/analysis-context` with repeated `source_id` query parameters taken only from `workspace.sources[].source_id`; use its returned `sources[]` and do not invent source records.

Add one original, accessible source-model destination within the existing destination system. Render each `workspace.sources[]` membership once using `source_id`, `alias`, `role`, and `position`; render each relationship once using `relationship_id`, `left_source_id`, `right_source_id`, `field_pairs`, `cardinality`, `join_behavior`, `filter_direction`, `is_active`, `is_confirmed`, `validation_state`, `diagnostics`, `version`, and `validated_at`. Trust copy must be literal: only active, confirmed, valid relationships may appear executable. Suggested, inactive, unconfirmed, invalid, stale, blocked, and `many_to_many` records must be visibly non-executable. Include clear loading, empty-workspace, no-relationships, managed-source-unavailable, and general API-error states.

The non-negotiable boundary is read-only live rendering. Do not add relationship creation, editing, deletion, activation, candidate profiling, workspace membership mutation, AI Chat request changes, automatic path selection, backend edits, unrelated destination redesign, or any `GEMINI.md` change. Do not copy Power BI visual assets or layout. Creative latitude includes component composition, node and connector styling, accessible keyboard focus, spacing, motion, concise trust copy, and responsive treatment within the existing AI_Tool design system.

Acceptance requires the current upload workspace to survive destination changes during the session; source nodes and relationship connectors to match live IDs and endpoints exactly; trust states to remain honest; all empty/loading/error states to be visible and screen-reader understandable; and existing Workspace, Explore, Dashboards, AI, upload, and Data Hub behavior to remain intact.

For repair acceptance, include `!rel.is_suggested` in the executable predicate so every suggested relationship uses the non-executable treatment and its visible status includes `Suggested`, even when it is active, confirmed, freshly valid, and not many-to-many. Preserve all accepted source-model behavior and make no other source changes. Run `python .codex/hooks/agent_harness_check.py`, `git diff --check`, and `npm --prefix frontend\frontend run build`. Do not claim browser acceptance; return the exact changed file and verification output to Codex, then stop.
