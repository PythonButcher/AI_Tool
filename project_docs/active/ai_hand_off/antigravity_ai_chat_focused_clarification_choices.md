# AI Chat Focused Clarification Choices

**IMPLEMENTATION COMPLETE — USER BROWSER ACCEPTANCE PENDING**

## Verified Implementation State

`frontend/frontend/src/features/ai/AIShell.jsx` resolves scoped request context from `session_state.dataset_context.dataset`. Data Hub requests preserve that canonical object as `dataset_ref` and omit unrelated global inline rows and semantic state.

Clarification choices are disabled while a request is in flight and become resolved only after a successful response. A failed request retains the pending clarification so the user can retry. Build, agent-harness, whitespace, and Codex source-review gates are complete. The user exclusively owns the remaining browser acceptance.

Goal: Render the backend-owned focused decision clarification in AI Chat and let the user answer one objective-metric question without leaving the conversation.

Work only in `frontend/frontend/src/features/ai/AIShell.jsx` and, if styling is needed, `frontend/frontend/src/features/ai/AIShell.css`. Read `project_docs/INDEX.md`, `project_docs/active/README.md`, `project_docs/active/status/decision_intelligence_execution_status.md`, `project_docs/active/rules/CODEX_FRONTEND_GUARDRAIL_READ_FIRST.md`, and the Decision Chat Conversational Continuity section of `project_docs/active/contracts/decision_objects.md` before editing.

The backend classification is `backend_contract_ready`. `POST /api/decision/chat/turns` returns top-level `clarification_state`. A pending state uses `schema_version: "di_clarification_v1"`, `status: "pending"`, `question_id: "objective_metric"`, `prompt`, `response_kind: "single_choice_or_exact_text"`, `choices[]` with `choice_id`, `label`, and `description`, `accepts_text`, and `text_constraint`. The same pending object is stored in `session_state.clarification_state`. The response turn that applies an answer returns top-level `clarification_state.status: "resolved"`, `choice_id`, `summary`, and `next_question`, plus the corrected `session_state`, `decision_output.frame`, and `decision_output.correction_state`.

Add `clarification_state: data.clarification_state || null` to the assistant message object created from a successful turn. When that state is pending, show the backend `prompt` and enabled choice buttons in the existing AI Chat message flow. Do not replace the current answer, workspace preview, result pane, missing-input display, or normal text composer. A choice click must submit a normal turn to `POST /api/decision/chat/turns` with the same current `dataset`, `semantic_model`, `dataset_ref`, bounded `conversation_history`, and scoped `session_state`, plus `user_message` equal to the choice label and `clarification_response: { choice_id }`. Use the returned assistant message, artifacts, session state, decision readiness, capability state, and clarification state exactly as a normal successful turn does. Disable choices while the request is loading and surface the existing error treatment on failure.

Keep the text composer as the bounded text response path; the backend already accepts an exact metric label or choice ID in `user_message`. Do not infer choices from `missing_inputs`, semantic-model arrays, chat history, or workspace internals. Do not create correction payloads in React, do not call `/api/decision/chat/actions` for these buttons, and do not add persistence, refresh recovery, New Chat work, alternate comparisons, or other UI scope.

Acceptance requires an incomplete decision question to display the backend prompt and choices; selecting `Revenue` must add a normal user/assistant exchange, update the scoped session state, show the resolved objective metric and correction state, leave unrelated levers, segment, and time horizon intact, and prevent the answered question from being submitted again. Verify both the active inline dataset path and a Data Hub-scoped request at the request-payload level: the latter must retain the canonical Data Hub `dataset_ref` without attaching unrelated active rows. Existing free-text clarification, normal metric questions, follow-up refinements, chart results, decision actions, artifact inspection, and export behavior must remain functional. Run `npm --prefix frontend/frontend run build`, `python .codex/hooks/agent_harness_check.py`, and `git diff --check`.

Browser acceptance belongs to the user. Provide a short manual check asking the user to enter `How should we adjust discount rate by region next quarter?`, verify the focused metric choices appear, click `Revenue`, and confirm the updated decision frame stays in AI Chat with the existing lever, region segment, and next-quarter horizon preserved. Stop after this one behavior is implemented, built, documented truthfully, and ready for user browser acceptance. Do not edit backend files or any `GEMINI.md` file.
