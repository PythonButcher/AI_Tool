Goal: Execute explicitly selected, validated workspace relationships safely in AI Chat and chart analysis while preserving unchanged one-source behavior.

Target a new bounded relationship execution service under `backend/services/`, `backend/services/dataset_context.py`, `backend/decision_engine/chat_service.py`, `backend/routes/nlp_routes.py`, the semantic-model services required for namespaced fields, focused tests under `tests/`, and only the active contracts whose verified response fields change.

Read `AGENTS.md`, `project_docs/INDEX.md`, `project_docs/active/README.md`, `project_docs/active/status/project_execution_status.md`, `project_docs/active/active_gate/README.md`, `project_docs/active/data_sources/multiple_data_sources_implementation_plan.md`, `project_docs/active/contracts/multiple_data_source_workspace.md`, `project_docs/active/contracts/multiple_data_source_relationships.md`, `project_docs/active/contracts/data_catalog_lineage.md`, `project_docs/active/contracts/decision_objects.md`, `project_docs/active/rules/CODEX_FRONTEND_GUARDRAIL_READ_FIRST.md`, and `project_docs/active/codex_harness_engineering.md`.

Resolve `analysis_context` only from its workspace ID, current workspace version, ordered selected source IDs, primary source ID, and explicit selected relationship IDs. Compile only active, confirmed, freshly valid relationships from the same workspace. Refuse missing, cross-workspace, stale, blocked, cyclic, ambiguous, or unsupported many-to-many paths. Apply deterministic join order, composite keys, namespaced fields, unmatched-key diagnostics, and a documented row-expansion ceiling while preserving the primary-source grain contract.

Return source and relationship lineage, relationship versions, source fingerprints, field origins, join order, and observed fanout with every multi-source answer, table, and chart. Conversational refinements must retain the same verified analysis context. Legacy `dataset`, `dataset_ref`, and one-source analysis-context requests must remain behaviorally unchanged.

Do not add frontend code, relationship-canvas behavior, automatic relationship activation, silent path selection, unbounded row expansion, or changes to any `GEMINI.md` file. Run focused execution, Decision Chat, semantic, chart, source/workspace, and governance regressions plus both documentation gates and `git diff --check`. Codex must perform the source, contract, and test review in the same run before deciding whether a frontend handoff is justified.
