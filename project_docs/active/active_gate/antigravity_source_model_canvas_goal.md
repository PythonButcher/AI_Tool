Goal: Deliver a read-only source-model canvas that renders the current live workspace, its source memberships, and governed relationship trust without permitting edits.

Antigravity executes `project_docs/active/ai_hand_off/antigravity_source_model_canvas_goal.md` and stops after returning changed-file, build, and browser evidence to Codex. The implementation must use the exact live endpoints and response fields named in that handoff, preserve existing destinations and upload behavior, and leave relationship mutation, AI Chat multi-source payloads, backend code, and `GEMINI.md` outside scope.
