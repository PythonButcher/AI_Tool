# Codex Next Session Kickoff - AI Chat Decision Command Center

Goal: Turn the Next Implementation Cycle Priorities council into an executable active Decision Intelligence plan, starting with an AI Chat-native Decision Command Center backend contract that extends or wraps the existing `decision_output` artifact without breaking AI Chat answers, charts, exploration, artifact inspection, saved DecisionAssets, or exports.

Read `project_docs/INDEX.md`, `project_docs/active/README.md`, `project_docs/active/status/decision_intelligence_execution_status.md`, `project_docs/active/rules/CODEX_FRONTEND_GUARDRAIL_READ_FIRST.md`, `project_docs/active/contracts/decision_objects.md`, `project_docs/active/agent_council/README.md`, `project_docs/active/agent_council/outputs/next-implementation-cycle-priorities/README.md`, `project_docs/active/agent_council/outputs/next-implementation-cycle-priorities/2026-06-28-next-implementation-cycle-priorities-summary.md`, and `project_docs/active/agent_council/outputs/next-implementation-cycle-priorities/2026-06-28-next-implementation-cycle-priorities-council.json`.

Inspect the source boundary before planning edits: `backend/services/decision_output_service.py`, `backend/decision_engine/chat_service.py`, `backend/services/decision_asset_service.py`, `backend/services/decision_graph_service.py`, `frontend/frontend/src/features/ai/AIShell.jsx`, `frontend/frontend/src/utils/decisionPdfExport.js`, `tests/test_decision_chat_service.py`, `tests/test_decision_workspace_service.py`, `tests/test_decision_reliability_benchmark.py`, `tests/test_decision_graph_service.py`, and `tests/test_decision_asset_service.py`.

Decide whether the command-center state should be an additive `decision_output.command_center` section or a separate wrapper artifact that carries the existing `decision_output` unchanged. The decision must be source-backed. Use the current `decision_output` contract fields first: `frame`, `dataset_trust`, `readiness`, `evidence_board`, `decision_map`, `scenario_compare`, `source_refs`, `export_sections`, and `truth_boundary`. Add only the minimum new fields needed for one coherent command-center view, such as section ordering, stale or rerun state, allowed next checks, disabled next checks with reasons, export readiness, and limitations.

Keep the work Codex-owned until backend truth is ready. Do not edit frontend implementation files unless the user explicitly authorizes Codex frontend work in the new session. If frontend work is needed after the backend contract is defined or verified, create a bounded Gemini or Antigravity handoff file with exact fields, allowed actions, disabled states, acceptance checks, build command, and manual browser checklist.

The plan must preserve product truth. The command center must not present final recommendations, predictions, simulations, optimizers, causal proof, autonomous decisions, live saved-asset refresh, or unsupported ML behavior. Scenario Compare remains bounded direct adjustment only. Saved DecisionAssets remain immutable historical snapshots.

Produce the smallest useful implementation plan first. A good first vertical path is AI Chat decision prompt to structured command-center payload to tests to a focused frontend-agent handoff. If backend code changes are necessary, update `project_docs/active/contracts/decision_objects.md` before or with implementation, keep changes additive, and run the narrowest relevant tests.

Acceptance checks: the contract decision is documented, the active plan or handoff is created in the correct project docs location, existing AI Chat `decision_output` behavior remains compatible, unsupported capability prompts remain observational-only, saved DecisionAsset snapshot semantics remain intact, export sections do not become raw field dumps, and the next frontend owner does not have to infer backend truth.

Verification commands when applicable: run `python .codex/hooks/agent_harness_check.py`, `git diff --check`, and the focused backend tests touched by the plan. If `decision_output` or chat behavior changes, run `python -m unittest tests.test_decision_chat_service` and the relevant reliability benchmark path. If graph actions or saved assets are touched, run `python -m unittest tests.test_decision_graph_service` or `python -m unittest tests.test_decision_asset_service` respectively.

Before final response, state the project gate plainly: backend contract plan ready, backend implementation verified, frontend-agent handoff ready, blocked, or awaiting user browser acceptance. If another agent acts next, put the forward-looking `Goal:` prompt in a handoff file and reference that file instead of pasting it into chat.
