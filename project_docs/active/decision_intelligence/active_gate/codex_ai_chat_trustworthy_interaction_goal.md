Goal: Establish the source and backend foundation for trustworthy AI Chat interaction, then prepare only the first bounded frontend handoff proven necessary by the audit.

Read `project_docs/INDEX.md`, `project_docs/active/README.md`, `project_docs/active/status/decision_intelligence_execution_status.md`, `project_docs/active/decision_intelligence/active_gate/README.md`, `project_docs/active/decision_intelligence/active_gate/phase_1_ai_chat_trustworthy_interaction_plan.md`, `project_docs/active/rules/CODEX_FRONTEND_GUARDRAIL_READ_FIRST.md`, `project_docs/active/contracts/decision_objects.md`, and `project_docs/active/contracts/data_catalog_lineage.md`. Use Release 1 in `project_docs/active/future/codex/ai_chat_decision_intelligence_user_outcome_audit_and_repair_plan.md` as the approved planning source. Read `project_docs/active/codex_harness_engineering.md` before broad source inspection.

Inspect `backend/decision_engine/chat_service.py`, `backend/decision_engine/mode_detection.py`, `backend/routes/decision.py`, `tests/test_decision_chat_service.py`, and `tests/test_decision_workspace_service.py`. Inspect `frontend/frontend/src/features/ai/AIShell.jsx`, `AIShell.css`, `DecisionOutputReview.jsx`, `DecisionCommandCenter.jsx`, `frontend/frontend/src/context/DataContext.jsx`, `frontend/frontend/src/context/WarehouseContext.jsx`, `frontend/frontend/src/utils/mentionUtils.jsx`, and `frontend/frontend/src/features/business/decision/decisionApi.js` only to trace the integration and prove frontend gaps. Do not edit frontend files or any `GEMINI.md` file in this session.

Trace the exact behavior of `/api/decision/chat/turns` and `/api/decision/chat/actions`, including `dataset`, `dataset_ref`/`datasetRef`, `resolved_datasets`, `dataset_trust`, `conversation_history`, `session_state.active_mode`, `mode_context.current_mode`, `mode_context.reason_code`, `suggested_actions[].action_id`, `suggested_actions[].enabled`, `suggested_actions[].disabled_reason`, and `session_state.draft_workspace`. Confirm how the active frontend dataset and any `@` dataset mention become the payload sent to Decision Chat. Use source and active contracts as truth; do not invent a second state model or broaden this work into the deferred multi-source architecture.

Implement the backend-owned portions needed to guarantee truthful dataset identity, explicit-mode precedence, ambiguity signaling, action-catalog and handler parity, and current-state `open_workspace`. Add focused regressions for dataset mismatch or refusal, explicit mode override, ambiguous chart-versus-decision comparison, enabled action execution, and stale-workspace prevention. Update the active contract when the public payload or response changes.

After source and tests establish backend readiness, classify the remaining work as `backend_not_ready`, `backend_contract_ready`, or `frontend_repair_only`. If a frontend gap remains, create one focused `Goal:` handoff under `project_docs/active/ai_hand_off/` covering only the first independently reviewable behavior. Name exact endpoint fields, target React files, acceptance checks, `npm --prefix frontend/frontend run build`, and the required user browser check. Keep session persistence free of raw dataset rows and ensure `New Chat` clears persisted structured state.

Acceptance requires proof that the selected or named dataset is the one analyzed, explicit mode is honored, ambiguous comparisons request confirmation, every enabled action has an executable path, `open_workspace` uses current structured state, and dataset changes cannot silently reuse a stale result. Run focused Decision Chat and workspace tests, `python .codex/hooks/agent_harness_check.py`, and `git diff --check`. Update the active status truthfully. Stop after the backend gate and first necessary frontend handoff are ready; Gemini or Antigravity owns frontend implementation, frontend build verification, and browser work, and the user owns final browser acceptance.
