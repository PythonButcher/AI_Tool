# Gemini AI Chat Decision Command Center Handoff

Completed reference. Current status and next focus live in `project_docs/active/status/decision_intelligence_execution_status.md`.

Goal: Repair and finish the AI Chat Decision Command Center frontend implementation from the backend-owned `decision_output.command_center` contract inside the existing AI Chat decision output review path, without changing backend APIs or inventing frontend-only decision truth.

Read `project_docs/INDEX.md`, `project_docs/active/README.md`, `project_docs/active/status/decision_intelligence_execution_status.md`, `project_docs/active/rules/CODEX_FRONTEND_GUARDRAIL_READ_FIRST.md`, `project_docs/active/contracts/decision_objects.md`, and `project_docs/active/decision_intelligence/current/ai_chat_decision_command_center_backend_plan.md`.

Target files are `frontend/frontend/src/features/ai/AIShell.jsx`, new focused AI Chat decision review component files under `frontend/frontend/src/features/ai/`, and only the nearby AI Shell styling files if styling is needed. Do not edit backend files. Do not edit saved asset backend validation. Keep AI Chat answers, charts, exploration, artifact inspection, PDF export, and saved DecisionAsset reopen behavior intact.

Before adding command-center UI, break the oversized `AIShell.jsx` decision-output rendering path into focused components. Extract the `decision_output` review renderer into a dedicated component such as `DecisionOutputReview.jsx`, and extract the new command-center area into a smaller component such as `DecisionCommandCenter.jsx`. Keep data extraction, save/reopen callbacks, export callbacks, and AI Chat action dispatch wired through clear props from `AIShell.jsx`; do not move unrelated chat message rendering, chart rendering, library state, or global shell behavior in this slice. The goal is to stop growing `AIShell.jsx` while implementing the command center, not to rewrite the entire shell.

Repair requirements from source review: preserve `command_center` when saving a live `decision_output` as a DecisionAsset, so a newly saved and reopened asset can still render the command center from `artifact.command_center || artifact.content?.command_center`; remove generated scratch artifacts such as `patch.diff` and `scratch_replace.py` from the workspace; do not leave temporary rewrite scripts or patch dumps in the repo; keep `DecisionCommandCenter.jsx` focused enough that it does not become another monolithic replacement for `AIShell.jsx`.

Additional source-review blocker: `DecisionCommandCenter.jsx` must not render `allowed_next_checks` without a supported frontend handler as enabled buttons. If a check has `action_id: "analyze_workspace"`, it can call `handleActionClick("analyze_workspace", contextSessionState)`. If a check maps to an already implemented local action, wire that exact local action: `save_decision_snapshot` must use the existing save behavior, and `export_decision_output` must use the existing decision artifact export behavior. If no supported action exists, render the check as disabled or informational with the backend `description`, not as a clickable no-op.

Use `artifact.command_center || artifact.content?.command_center` as the command-center source. Render `status`, `stale_state`, `rerun_state`, `section_order`, `allowed_next_checks`, `disabled_next_checks`, `export_readiness`, `limitations`, and `truth_boundary`. Render the actual decision sections from the existing `decision_output` fields: `summary`, `dataset_trust`, `frame`, `readiness`, `evidence_board`, `decision_map`, `scenario_compare`, `source_refs`, and `export_sections`. Do not show the command center as raw JSON.

Allowed next checks are review or investigation controls only. If a check has `action_id: "analyze_workspace"`, it may call the existing AI Chat action path already used by the shell. If no supported action handler exists for a check, render it as a disabled or informational control rather than inventing a new API. Disabled checks must show their backend `reason`. Unsupported checks for simulation, optimization, autonomous decisioning, final recommendation, and live saved-asset refresh must stay disabled.

Preserve product truth. Do not present final recommendations, predictions, simulations, optimizers, causal proof, autonomous decisions, live saved-asset refresh, or unsupported ML behavior. Scenario Compare copy must say direct adjustment or sensitivity comparison only. Saved DecisionAssets must remain labeled as immutable snapshots when reopened.

Acceptance checks: `AIShell.jsx` no longer contains the full inline `decision_output` review body; current live `decision_output` artifacts show a command-center header or control area when `command_center` is present; older saved or live artifacts without `command_center` still render through the existing Decision Review layout; export still uses `export_sections` and does not dump command-center fields; saving a current command-center `decision_output` includes `command_center` in the payload and reopening that saved DecisionAsset still shows the command center; enabled next-check buttons only appear for actions that actually execute; checks without supported handlers are disabled or informational with backend text; disabled next checks display backend reasons; unsupported advanced capabilities are visibly disabled; generated scratch files are removed; no Decisions-window navigation is required.

Run `npm --prefix frontend\frontend run build`, `git diff --check`, and `python .codex/hooks/agent_harness_check.py`. Manual browser checklist for the user: open AI Chat, ask a decision prompt that produces `decision_output`, confirm the command-center area appears, run or inspect observational analysis if available, verify disabled unsupported checks show reasons, export the decision output, save the decision asset, reopen it from the library, and confirm it is still labeled as an immutable observational snapshot.
